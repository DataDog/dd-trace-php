# Usage

From the current directory:

    docker-compose up -d

    curl localhost:8888

Then you can look at the profile output into in `./cachegrind-files`.

You will need a tool like kcachegrind or qcachegrind to inspect the profiling output.
