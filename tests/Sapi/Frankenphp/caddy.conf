{
	# Enable FrankenPHP
	frankenphp {
        worker {
            file {{frankenphp_php}}
            {{env}}
        }
	}
	# Configure when the directive must be executed
	order php_server before file_server
}

{{frankenphp_host}}:{{frankenphp_port}} {
	# Execute PHP files in the current directory and serve assets
	php_server
}
