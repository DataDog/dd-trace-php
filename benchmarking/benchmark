#!/usr/bin/env php
<?php

use DDTrace\Benchmark\Commands\RunBenchmarks;
use DDTrace\Benchmark\Runner;

require __DIR__ . '/vendor/autoload.php';

$app = new Runner('DDTrace Benchmark');
$command = new RunBenchmarks();
$app->add($command);
$app->setDefaultCommand($command->getName());
$app->run();
