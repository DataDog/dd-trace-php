Bazel 9.2 default output paths are relative to top-level option baselines. Runtime configuration checksums converge across product/debug/LTO flags, and a matrix contains one core per ABI; changing command-line flags between invocations can still rename output paths and miss the cache. Benchmarks must keep flags identical. A tested legacy-naming experiment introduced duplicate configurations and was reverted; no global naming changes are retained.
